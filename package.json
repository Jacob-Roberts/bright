{
  "private": true,
  "workspaces": {
    "packages": [
      "lib",
      "web"
    ]
  },
  "scripts": {
    "dev": "yarn workspace @autovation/bright watch & yarn workspace bright-web dev",
    "changeset": "changeset",
    "version": "changeset version",
    "build": "yarn workspace @autovation/bright build",
    "release": "yarn build && changeset publish"
  },
  "repository": "code-hike/bright",
  "author": "pomber",
  "dependencies": {
    "@changesets/cli": "^2.26.0"
  }
}
